# Copy this file to .env and fill in your deployed Worker API URL.
# Local dev without it still works in offline/demo mode (localStorage).
# The app + API are served from the same Worker, so this is normally
# just the Worker URL itself:

VITE_API_URL=https://cadensiq.celestsolutions.workers.dev
